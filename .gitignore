/var/
/vendor/
/node_modules/
/public/build/
/public/bundles/
/.env.local
/.env.*.local

# uploads (persistant, pas dans git)
/public/uploads/

# uploads (persistant, pas dans git)
/public/uploads/

# fichiers uploadés (ne pas versionner)
/uploads/
/public/uploads/

# uploads (persistants, pas dans git)
/public/uploads/
