{% if c.pdfPath %}
  <a class="btn btn-sm btn-outline-secondary"
     href="{{ path('app_administrateur_convention_pdf', { entite: entite.id, id: c.id }) }}"
     target="_blank">
    <i class="bi bi-filetype-pdf"></i> PDF
  </a>
{% else %}
  <span class="text-muted small">—</span>
{% endif %}
